node_modules/
dist/
.next/
coverage/
*.tsbuildinfo

__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

.env
.env.local
.DS_Store

# Embedding and replay caches. Content-addressed and regenerable; the
# embedding model is local and free, so refilling costs nothing at all.
# Replay response cache. Content-addressed and reproducible, but ~10s of MB and
# regenerable, so it is not committed. Deleting it only costs money to refill.
.cache/

# Human labels are real measurement data and ARE committed (calibration/labels.jsonl).
# Nothing to ignore here; this note exists so nobody adds it later by reflex.
